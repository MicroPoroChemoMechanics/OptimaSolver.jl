[deps]
ChemistryLab = "f346e401-14f9-407c-a0e2-c4f64a3e574a"
DynamicQuantities = "06fc5a27-2a28-4c7c-a15d-362465fb6821"
JSON = "682c06a0-de6a-54ab-a142-c8b1cf79cde6"
OptimaSolver = "24efeca9-a785-406e-adb3-83609348bee7"
Printf = "de0858da-6303-5e67-8744-51eddeeeb8d7"

# The solver under test is THIS checkout, never the registered release: a
# benchmark that silently loads the registry's OptimaSolver measures the wrong
# code and reports it as a result. ChemistryLab is the sibling checkout because
# it is what builds the cement case the numbers are about.
[sources]
ChemistryLab = {path = "../../ChemistryLab.jl"}
OptimaSolver = {path = ".."}
